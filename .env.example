# Copy this file to .env and fill in your Supabase project's values
# (Settings -> API in the Supabase dashboard). Never commit the real .env.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Only needed locally to run `npm run seed` (never used by the browser build).
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
